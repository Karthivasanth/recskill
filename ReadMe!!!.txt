after opnening venv 
run pip install -r req.txt    
then navigate to src to run django runserver
if u install any other run dependency pip freeze > req.txt[not on src folder do it outside of it]
(note:postgre and django are already installed)
have fun
